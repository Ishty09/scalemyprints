{
  "name": "@scalemyprints/workers",
  "version": "0.1.0",
  "private": true,
  "description": "Python FastAPI workers - marker package for Turborepo",
  "scripts": {
    "dev": "uv run uvicorn scalemyprints.main:app --reload --port 8000",
    "build": "echo 'Workers built via Docker; see Dockerfile'",
    "lint": "uv run ruff check src tests",
    "lint:fix": "uv run ruff check --fix src tests && uv run ruff format src tests",
    "test": "uv run pytest",
    "test:watch": "uv run ptw",
    "type-check": "uv run mypy src",
    "clean": "rm -rf .pytest_cache .ruff_cache .mypy_cache htmlcov .coverage dist"
  }
}
